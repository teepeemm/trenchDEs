% !TEX TS-program = LuaLaTeX-dev
\DocumentMetadata{
  tagging=on,
  lang=en-US,
  pdfstandard=UA-2,
  tagging-setup={
    math/alt/use,
    math/setup=mathml-SE
  }
}
\documentclass{article}
\includeonly{}
\usepackage{../wtrench}
\input{../header}
\begin{document}

\section{Section}
\exercises
Text

%\begin{exerciselist}
%\item exercise
%\end{exerciselist}

{\itshape If $a$, $b$, $c$, and $\alpha$ are constants, then
\[
a(e^{\alpha x})''+b(e^{\alpha x})'+ce^{\alpha x}=(a\alpha^2+b\alpha+c)e^{\alpha
x}.
\]
Use this in Exercises~16--21 to find a
particular solution. Then find the general solution
and, where indicated, solve the initial value problem
and graph the solution.}

%\begin{exerciselist}[resume]
%\item exercise
%\end{exerciselist}
\end{document}
